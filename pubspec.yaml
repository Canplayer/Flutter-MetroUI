name: metro_ui
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # To add assets to your package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package

  assets:
    - assets/ic_back.svg

  fonts:
    # Segoe WP 字体集（Windows Phone 7 系统字体），全部归入一个家族，用字重区分。
    # 各文件实际 usWeightClass：Light=300 / SemiLight=350（非标准，pubspec 只接受
    # 100–900 整百值故按 300 注册）/ Regular=400 / Semibold=600 / Bold=700 /
    # Black=800（字体名是 Black，但实际 usWeightClass=800）。
    # 注意：SemiLight 与 Light 同为 300，同家族同字重的多个文件，引擎按注册顺序
    # 匹配（先注册者优先）；Dart 中请求 FontWeight(350) 时会取最接近的已注册字重。
    - family: Segoe WP
      fonts:
        - asset: fonts/SegoeWP-Light.ttf
          weight: 300
        - asset: fonts/SegoeWP-Semilight.ttf
          weight: 300
        - asset: fonts/SegoeWP.ttf
          weight: 400
        - asset: fonts/SegoeWP-Semibold.ttf
          weight: 600
        - asset: fonts/SegoeWP-Bold.ttf
          weight: 700
        - asset: fonts/SegoeWP-Black.ttf
          weight: 800